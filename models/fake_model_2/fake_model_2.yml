version: 2

models:
  - name: fake_model_2
    owner: fake_person
    description: 'table with fake data to test, only emails .com'
    columns: 
      - name: id
      - name: first_name
      - name: last_name
      - name: email
      - name: gender
      - name: ip_address