# packages:
#   - package: dbt-labs/dbt_utils
#     version: 0.9.2

# dbt debt --> run packages
# ALERT! dbt utils is only suported < dbt version 2.0.0
# ineed eliminate folder dbt_utils to run dbt docs and dbt seed

# https://hub.getdbt.com/
# https://docs.getdbt.com/docs/build/packages
